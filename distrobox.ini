[nodejs_dev]
image=ubuntu:latest
additional_packages="nodejs npm git build-essential"
init_hooks='getent group $(id -un) || sudo groupadd --gid $(id -g) $(id -un) && getent passwd $(id -un) || sudo useradd --uid $(id -u) --gid $(id -g) --shell /bin/bash --create-home $(id -un)'